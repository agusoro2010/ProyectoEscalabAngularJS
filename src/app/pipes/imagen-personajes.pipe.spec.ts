import { ImagenPersonajesPipe } from './imagen-personajes.pipe';

describe('ImagenPersonajesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenPersonajesPipe();
    expect(pipe).toBeTruthy();
  });
});
